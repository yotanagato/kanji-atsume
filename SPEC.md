# かんじあつめ（kanji-atsume）仕様

小学1年生の配当漢字・英単語・九九を、**さかなつり／カード**の2つの遊びかたで覚える知育Webアプリ。
こども向け。単一HTML完結・依存ゼロ・オフライン動作（PWA）。

## マス（item）＝ この アプリの背骨

**おぼえた/おぼえていない を管理する最小単位が「マス」**で、`id` がそのまま保存キーになる。

| 教科 | マスの作り方 | マス数 |
| --- | --- | --- |
| かんじ | 80字 × 3タイプ（`kanji:山:y2k` / `:k2y` / `:word`） | 240 |
| えいご | 52語 × 2タイプ（`eigo:cat:ja2en` / `:en2ja`） | 104 |
| くく | 1×1〜9×9（`kuku:3x7:mul`） | 81 |
| | | **合計 425** |

「読めるけど書けない」「単独ではわかるが熟語だと選べない」を別々に追うため、
**漢字は1字1マスではなく、出題タイプごとに別のマス**にしてある（ここを1字1マスにすると、
よみ→かんじ だけ3回正解した時点でその字が出題されなくなり、覚えた判定が甘くなる）。

### 教科を足すときにやること

1. `SUBJECTS` と `QTYPES` に足す
2. `buildAllItems()` でマスを作る
3. `make〇〇Question(q, item)` を書いて `makeQuestion()` から呼ぶ
4. 図鑑に一覧を足す（`renderZukan〇〇`）

**出題・判定・魚・カードの描画は `choice.text` / `choice.kind` / `choice.correct` しか見ない**ので、
それ以外は触らなくてよい。`kind` は `kanji`（明朝）/ `yomi` / `en` / `num` で書体が決まる。

- 場所: `C:\Users\yotan\OneDrive\Desktop\claude\kanji-atsume\`
- 構成: `index.html`（HTML/CSS/JS 全部入り）＋ `manifest.json` / `sw.js` / `icon-192.png` / `icon-512.png` / `README.md`
- 公開: https://yotanagato.github.io/kanji-atsume/ （GitHub Pages / repo `yotanagato/kanji-atsume` / main のルート）
- dev server: `.claude/launch.json` の `kanji-atsume`（python http.server, port 8646）
- 雛形: `eigo-yomi` / `tokei-gakushu`（sound・confetti・store・showScreen・ゲートを流用）、
  ねこ（ハチワレ）と魚の描画は `kanji-fishing/cat.js` `fish.js` から移植

## 遊びかた（モード）

`settings.mode` … `"fish"`（既定）/ `"card"`。ホームの「あそびかた」チップで切り替える。

**出題・正誤判定・ライトナー箱・図鑑・記録は完全に共通**で、モードは見た目だけを差し替える。
モードを足すときに触るのは `renderQuestion()` の分岐と、`answer()` / `afterCorrect()` を呼ぶUI側だけ。

- **さかなつり**: 選択肢を魚にして泳がせる。1匹1レーンに固定して重ならないようにしてある
  （重なると押し間違いが起きるため）。ねこが褒める／励ます。canvas は CSSピクセル基準＋DPR変換。
  端末回転やペインのリサイズを resize イベントだけでは取りこぼすので、
  ループ内で15フレームごとに実寸を見て、変わっていたら作り直している。
- **カード**: 4択のボタン。

### 魚に乗せる字の書体

`fishFont(text, isKanji)`。**文字数ではなく「漢字か、よみか」で切り替える**こと。
文字数で切り替えると、かんじ→よみ の選択肢で「て」だけ明朝・「ちから」はゴシック、という
ちぐはぐな見た目になる（実機で指摘を受けて修正した）。長さは字の大きさだけで吸収する。

### 描画ループを止めないための決まり

canvas の描画中に例外が出ると requestAnimationFrame が止まり、
**背景ごと真っ青で魚が出ない**状態になる（実際に起きた。原因はパーティクルの半径が負）。

- パーティクルは **`life` を減らしてからふるいにかける**。逆にすると負の半径が `arc()` に渡って例外
- `loop()` は `try/catch` で包む。1コマ落ちてもゲームは続ける
- `start()` は毎回 `cancelAnimationFrame` してから張り直す（`running` フラグを信用しない）
- 1秒コマが進んでいなければ復帰させる見張り番＋`visibilitychange` での復帰も入れてある

## 漢字データ

`KANJI` 配列（index.html 内）に80字。文部科学省「学年別漢字配当表」1年の80字と完全一致することを検証済み。

各要素:

| key | 内容 |
| --- | --- |
| `k`  | 漢字 |
| `y`  | 出題に使う代表よみ（ひらがな。送りがなは `はや(い)` 形式） |
| `ys` | 図鑑に出す読みのバリエーション |
| `e`  | ヒント絵文字 |
| `lv` | レベル 1〜4（各20字。1がやさしい） |
| `g`  | なかま（かず／しぜん／からだ／ひと／まち／うごき／いろ） |
| `w`  | ことば `[[漢字表記, よみ], ...]`（**その漢字を必ず含める**。ことば問題で □ に置き換える） |

データを足す・直すときの不変条件（`index.html` を開いた状態でコンソール実行して確認できる）:

- `w[0]` に `k` が含まれる（含まれないと □ が作れない）
- `y` と `w[*][1]` はひらがな（＋送りがなの括弧）のみ。ローマ字を混ぜない
- 80字に重複なし

## 出題

ホームのチップで複数選択（最低1つは残る）。1セットの問題数は 5/10/15/20/30/50/100 から選ぶ
（10問を超えると進み具合は丸ではなく「3 / 50」の数字表示になる）。

**かんじ**

1. **よみ→かんじ** … 絵文字＋よみを見せて、漢字4つから選ぶ
2. **かんじ→よみ** … 漢字を見せて、よみ4つから選ぶ（つりモードでは魚にひらがなが乗る）
3. **ことば** … `立ち上がる` の一字を □ にして、漢字4つから選ぶ（よみ仮名は表示する）

ダミー（`kanjiDistractors()`）:

- 同じ「なかま」の漢字を優先し、足りなければ他から補う
- **かんじ→よみ では、同じ読みの漢字を除外する**（日と火、木と気 は答えが2つになってしまう）
- **ことば問題では、問題文にすでに出ている漢字を除外する**（`□ち上がる` の選択肢に `上` を出さない）

**えいご**（`eigo-yomi` の語彙から、4択にならない機能語8語を除いた52語）

4. **にほんご→えいご** … 絵文字＋日本語を見せて、英単語4つから選ぶ
5. **えいご→にほんご** … 英単語を見せて、日本語4つから選ぶ。
   **絵文字は出さない**（答えそのものになる）。**英語ボイスが無い環境では読み上げない**
   （日本語ボイスに英語を読ませるとカタカナ読みになり、学習の妨げになる。eigo-yomi と同じ方針）

**くく**（1×1〜9×9。だんで絞り込める）

6. **かけざん** … `7 × 2` を見せて、数字4つから選ぶ。
   ダミーは①段ちがい（`ans±a` `ans±b`）②1〜2ちがい③まったく別の九九の答え、を1つずつ。
   **全部を近い数にすると見分けがつかず苦行になる**ので、この配合を崩さないこと

## 出題の振り分け（ライトナー箱）

漢字ごとに箱1〜3。抽選の重みは **box1=6 / box2=3 / box3=1**（`eigo-yomi` と同じ）。

- 正解 → 箱を1つ上げる（最大3）
- 不正解 → 箱1に戻す。**減点はしない**
- 「にがて」＝ 箱1 かつ 1回でも間違えた漢字

**まちがえても次に進まない。正解するまで同じ問題に挑戦できる**（`answer()`）。

- 成績（星・箱・⭕🔁・日別記録）に残すのは **1回目の答えだけ**（`recordFirstTry()`）。
  2回目以降で正解しても星は増えず、箱も上がらない＝「にがて」として残る
- まちがえた選択肢はその場で引っ込む（カードは `disabled`、魚は灰色になって消える）ので、
  詰まっても最後は必ず正解にたどり着ける
- まちがえると出題の音声をもう一度流す（`repeatPrompt()`。かんじ→よみ は答えになるので流さない）

1セット5問。1回目で正解すると星1つ。星10個でスタンプ1つ（絵文字24種からランダム）。

## ストレスをかけない設計（tokei-gakushu からの方針継承）

- 不正解でも減点なし・音は叱る音にしない（やわらかい上向き2音）
- ねこは「ちがうよ〜 もういちど！」と促すだけで、できたら必ず褒める
- 「やめる」でいつでも中断できる

## 音（iOS の注意）

効果音は WebAudio（外部ファイルなし）。**iPhone では次の2つが無いと効果音だけ鳴らない。**

- 最初のタップで `AudioContext` を作って `resume()` する（`sound.unlock()` を
  document の pointerdown / touchend / click に付けてある）
- `navigator.audioSession.type = "playback"` を指定する。これが無いと
  **着信スイッチ（マナーモード）で WebAudio だけ消される**。
  読み上げ（SpeechSynthesis）は鳴るので「声は出るのに効果音が出ない」という症状になる

## よみあげ

Web Speech API（日本語ボイス）。開発機には日本語4種（Ayumi / Haruka / Ichiro / Sayaka）が入っている。

- 送りがなの括弧は読み上げ前に除去する（`はや(い)` → `はやい`）
- 「よみ→かんじ」「ことば」は出題時に自動読み上げ（設定でOFF可）
- 「かんじ→よみ」は**答え合わせのタイミングで**読む（出題時に読むと答えになってしまう）

## 保存

`localStorage["kanji-data"]` の1キーのみ。`{ stars, stamps, box, stat, days, settings }`。
設定（読み上げON/OFF・速さ・声・選んだレベル/なかま/出題タイプ）も同じキーに入る。

## おとなゲート → せってい

ホームの「おとなのひと」→ 一桁の足し算に正解すると設定画面。
設定は 読み上げON/OFF・はやさ・こえ・データ全消去（2回タップで確定）。

## 公開（GitHub Pages）

`kanji-atsume/` フォルダ自体が repo `yotanagato/kanji-atsume`（public）。main のルートを Pages が配信する。
更新は add → commit → push するだけで反映される（反映まで数十秒）。

- 相対パス（`./`）しか使っていないので、サブディレクトリ配信でもそのまま動く
- `sw.js` はネットワーク優先なので、push した内容はリロードで反映される
- **公開リポジトリなので、子どもの名前・実パス・個人情報をファイルに書かない**

## 図鑑

教科タブ制（`zukanTab`）。

- **かんじ** … 80マス。3タイプぜんぶ box3 で金色⭐、途中は `1/3` `2/3` を隅に出す
- **えいご** … 52語の一覧。2タイプの達成を ⭐🔵🔁・ で表示
- **くく** … 9×9の表。マスの色が箱の状態

## 未決事項

- ボイス。いまは Windows の日本語ボイス（SAPI）。VOICEVOX のずんだもんを wav で同梱する案は保留
  （ひらがなが読めるので優先度低め、と判断）
- 書き順アニメ／なぞり書きは未実装（要望が出たら別モードとして追加。80字ぶんの筆順データが要る）
- 図鑑の並びはレベル順。五十音順・画数順の切り替えは未実装
- 習熟度に応じて選択肢を4→6に増やす等の難易度調整
